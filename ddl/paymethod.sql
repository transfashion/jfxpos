-- DDL Table PAYMETHOD (FirebirdSQL 5.0.4)
CREATE TABLE PAYMETHOD (
    PAYMETHOD_ID INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    PAYMETHOD_NAME VARCHAR(100) NOT NULL,
    PAYMETHOD_DISC DECIMAL(15, 2) DEFAULT 0.00 NOT NULL,
    PAYMETHOD_MINPAYMENT DECIMAL(15, 2) DEFAULT 0.00 NOT NULL,
    ALLOWMULTIPLE BOOLEAN DEFAULT FALSE NOT NULL,
    PAYMDEV_ID INTEGER,
    PAYMDEVTYPE VARCHAR(15) NOT NULL,
    IS_ACTIVE BOOLEAN DEFAULT TRUE NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    MODIFIED_AT TIMESTAMP,
    DATATIMESTAMP TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    
    CONSTRAINT FK_PAYMETHOD_PAYMDEV FOREIGN KEY (PAYMDEV_ID) REFERENCES PAYMDEV (PAYMDEV_ID),
    CONSTRAINT CHK_PAYMETHOD_DEVTYPE CHECK (PAYMDEVTYPE IN ('EDC', 'QR', 'VOU', 'ADV', 'CASH'))
);

-- Indexing for performance
CREATE INDEX IDX_PAYMETHOD_DEVTYPE ON PAYMETHOD(PAYMDEVTYPE);

-- Sample Data PAYMETHOD
-- Cash Payment
INSERT INTO PAYMETHOD (PAYMETHOD_NAME, PAYMETHOD_DISC, PAYMETHOD_MINPAYMENT, ALLOWMULTIPLE, PAYMDEV_ID, PAYMDEVTYPE)
VALUES ('TUNAI / CASH', 0.00, 0.00, TRUE, 6, 'CASH');

-- EDC Card Payments
INSERT INTO PAYMETHOD (PAYMETHOD_NAME, PAYMETHOD_DISC, PAYMETHOD_MINPAYMENT, ALLOWMULTIPLE, PAYMDEV_ID, PAYMDEVTYPE)
VALUES ('DEBIT BCA', 0.00, 10000.00, FALSE, 1, 'EDC');

INSERT INTO PAYMETHOD (PAYMETHOD_NAME, PAYMETHOD_DISC, PAYMETHOD_MINPAYMENT, ALLOWMULTIPLE, PAYMDEV_ID, PAYMDEVTYPE)
VALUES ('KREDIT BCA', 0.00, 50000.00, FALSE, 1, 'EDC');

INSERT INTO PAYMETHOD (PAYMETHOD_NAME, PAYMETHOD_DISC, PAYMETHOD_MINPAYMENT, ALLOWMULTIPLE, PAYMDEV_ID, PAYMDEVTYPE)
VALUES ('DEBIT MANDIRI', 0.00, 10000.00, FALSE, 2, 'EDC');

-- QRIS Payment
INSERT INTO PAYMETHOD (PAYMETHOD_NAME, PAYMETHOD_DISC, PAYMETHOD_MINPAYMENT, ALLOWMULTIPLE, PAYMDEV_ID, PAYMDEVTYPE)
VALUES ('QRIS', 0.00, 1.00, FALSE, 3, 'QR');

-- Voucher Payment
INSERT INTO PAYMETHOD (PAYMETHOD_NAME, PAYMETHOD_DISC, PAYMETHOD_MINPAYMENT, ALLOWMULTIPLE, PAYMDEV_ID, PAYMDEVTYPE)
VALUES ('VOUCHER BELANJA', 0.00, 0.00, TRUE, 4, 'VOU');
