-- DDL Table TRX (FirebirdSQL 5.0.4)
CREATE TABLE TRX (
    TRX_ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TRX_NO VARCHAR(50) NOT NULL UNIQUE,
    TRX_DATE TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    TOTAL_QTY INTEGER DEFAULT 0 NOT NULL,
    SUBTOTAL DECIMAL(15, 2) DEFAULT 0.00 NOT NULL,
    TOTAL_DISCOUNT DECIMAL(15, 2) DEFAULT 0.00 NOT NULL,
    TOTAL_TAX DECIMAL(15, 2) DEFAULT 0.00 NOT NULL,
    GRAND_TOTAL DECIMAL(15, 2) DEFAULT 0.00 NOT NULL,
    TOTAL_PAID DECIMAL(15, 2) DEFAULT 0.00 NOT NULL,
    TOTAL_RETURN DECIMAL(15, 2) DEFAULT 0.00 NOT NULL,
    CHANNEL_ID INTEGER,
    CASHIER_ID INTEGER,
	SITE_ID INTEGER,
	UNIT_ID INTEGER,
	STRUCT_ID INTEGER,
    IS_ACTIVE BOOLEAN DEFAULT TRUE NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    MODIFIED_AT TIMESTAMP,
    DATATIMESTAMP TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    
    CONSTRAINT FK_TRX_CHANNEL FOREIGN KEY (CHANNEL_ID) REFERENCES CHANNEL (CHANNEL_ID),
    CONSTRAINT FK_TRX_CASHIER FOREIGN KEY (CASHIER_ID) REFERENCES USERS (USER_ID)
);

-- Indexing for performance
CREATE INDEX IDX_TRX_NO ON TRX(TRX_NO);
CREATE INDEX IDX_TRX_DATE ON TRX(TRX_DATE);
